{
  "name": "mantenimiento",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "env-cmd -f .env --silent -- pnpm -r --parallel run dev",
    "start": "env-cmd -f .env --silent -- pnpm -r --parallel run start",
    "build": "pnpm -r run build",
    "studio": "env-cmd -f .env --silent -- pnpm -r --parallel run prisma:studio",
    "migrate": "turbo run prisma:migrate",
    "reset":"turbo run prisma:reset",
    "generate": "turbo run prisma:generate",
    "push": "turbo run prisma:push",
    "seed" : "turbo run prisma:seed", 
    "lint": "pnpm -r run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.25.0",
  "devDependencies": {
    "env-cmd": "^11.0.0",
    "turbo": "^2.8.10"
  },
  "dependencies": {
    "lucide-react": "^0.575.0"
  }
}